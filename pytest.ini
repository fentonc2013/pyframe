# pytest.ini (in C:\dev\pyframe\)
[pytest]
addopts = --html=output/reports/report.html --self-contained-html
testpaths = tests
markers =
    smoke: quick, high-value checks
    regression: broader coverage
    slow: long-running tests
